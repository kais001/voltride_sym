{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn" style="background-color: green; color: white; /* Add any other styles you want for the green button */">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
