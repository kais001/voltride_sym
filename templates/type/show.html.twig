{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1>Supprimer ce type ?</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>TypeName</th>
                <td>{{ type.typeName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_index') }}">back to list</a>



    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
