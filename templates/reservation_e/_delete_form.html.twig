<form method="post" action="{{ path('app_reservation_e_delete', {'id_e': reservation_e.id_e}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_e.id_e) }}">
    <button class="btn">Delete</button>
</form>
