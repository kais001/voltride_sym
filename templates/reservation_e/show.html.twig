{% extends 'baseF.html.twig' %}

{% block title %}ReservationE{% endblock %}

{% block center %}
    <h1>ReservationE</h1>
<div class="main-container">
    <div class="white-frame"> <!-- Div avec cadre blanc -->
            <div class="table-container"> <!-- Div pour centrer le tableau -->
    <table class="table">
        <tbody>
            <tr>
                <th>Id_e</th>
                <td>{{ reservation_e.id_e }}</td>
            </tr>
            <tr>
                <th>NbrPersonne</th>
                <td>{{ reservation_e.nbrPersonne }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ reservation_e.Commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_e_index') }}">back to list</a>

    <a href="{{ path('app_reservation_e_edit', {'id_e': reservation_e.id_e}) }}">edit</a>

    {{ include('reservation_e/_delete_form.html.twig') }}
    </div>
    </div>
    </div>
{% endblock %}
