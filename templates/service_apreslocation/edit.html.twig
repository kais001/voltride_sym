{% extends 'back/back.html.twig' %}

{% block body %}

{% block title %}Edit ServiceApreslocation{% endblock %}
<br>
<br>
<br>
<br>
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-header">
                        <h1 class="card-title">Edit Service Après location</h1>
                    </div>
                    <div class="card-body">
                        {{ include('service_apreslocation/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_service_apreslocation_index') }}" class="btn btn-secondary">Back to list</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
