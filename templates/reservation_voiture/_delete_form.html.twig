<form method="post" action="{{ path('app_reservation_voiture_delete', {'idR': reservation_voiture.idR}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_voiture.idR) }}">
    <button class="btn">Delete</button>
</form>
