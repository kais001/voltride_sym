{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block center %}
    <h1>Evenement</h1>
<div class="main-container">
    <div class="white-frame"> <!-- Div avec cadre blanc -->
            <div class="table-container"> <!-- Div pour centrer le tableau -->
    <table class="table">
        <tbody>
            <tr>
                <th>Id_event</th>
                <td>{{ evenement.id_event }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ evenement.type }}</td>
            </tr>
            <tr>
                <th>AdresseEvenement</th>
                <td>{{ evenement.adresseEvenement }}</td>
            </tr>
            <tr>
                <th>DateEvenement</th>
               <td>{{ evenement.dateEvenement ? evenement.dateEvenement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PlacesDispo</th>
                <td>{{ evenement.placesDispo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evenement_index') }}">back to list</a>

    <a href="{{ path('app_evenement_edit', {'id_event': evenement.id_event}) }}">edit</a>

    {{ include('evenement/_delete_form.html.twig') }}
    </div>
     </div>
      </div>
{% endblock %}
