{% extends 'base.html.twig' %}

{% block title %}ReservationB{% endblock %}

{% block body %}
    <h1>ReservationB</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdR</th>
                <td>{{ reservation_b.idR }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ reservation_b.dateDebut ? reservation_b.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ reservation_b.dateFin ? reservation_b.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ reservation_b.heureDebut ? reservation_b.heureDebut|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HeureFin</th>
                <td>{{ reservation_b.heureFin ? reservation_b.heureFin|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_b_index') }}">back to list</a>

    <a href="{{ path('app_reservation_b_edit', {'idR': reservation_b.idR}) }}">edit</a>

    {{ include('reservation_b/_delete_form.html.twig') }}
{% endblock %}
